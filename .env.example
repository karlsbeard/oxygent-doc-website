# OxyGent Documentation Environment Variables

# ==============================================
# Base Path Configuration
# ==============================================
#
# 自动检测逻辑（无需配置）：
# - 本地开发 (pnpm dev)：自动使用空字符串
# - GitHub Actions (CI=true)：自动使用 /oxygent-doc-website
#
# 手动覆盖示例：
# NEXT_PUBLIC_BASE_PATH=/oxygent-doc-website  # GitHub Pages 部署路径
# NEXT_PUBLIC_BASE_PATH=                      # 自定义域名（空字符串）
# NEXT_PUBLIC_BASE_PATH=/custom-path          # 自定义子路径

# ==============================================
# 常见使用场景
# ==============================================

# 场景 1: 本地开发（默认）
# 无需设置任何环境变量
# 运行: pnpm dev
# 访问: http://localhost:3000/

# 场景 2: 本地测试 GitHub Pages 构建
# NEXT_PUBLIC_BASE_PATH=/oxygent-doc-website
# 运行: pnpm build:test-gh-pages
# 预览: npx serve out (访问 http://localhost:3000/oxygent-doc-website/)

# 场景 3: GitHub Actions 自动部署
# 无需设置，CI=true 自动触发 /oxygent-doc-website
# 部署: git push origin master

# 场景 4: 自定义域名部署
# NEXT_PUBLIC_BASE_PATH=
# 运行: pnpm build
# 部署到根路径

# ==============================================
# 其他配置
# ==============================================

# Node 环境（通常由系统自动设置）
# NODE_ENV=development  # 开发模式
# NODE_ENV=production   # 生产模式

# CI 环境检测（由 GitHub Actions 自动设置）
# CI=true  # 表示在 CI 环境中运行
